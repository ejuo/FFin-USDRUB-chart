# USD/RUB: Freedom Bank и USDRUBF

Адаптивный статический сайт для внутридневного сравнения:

- курса покупки и продажи USD/RUB в мобильном приложении Freedom Bank;
- часовых свечей вечного фьючерса `USDRUBF`, страницу которого показывает РБК Quote.

Цель — найти день и час, когда разрыв между биржевым ориентиром и курсом исполнения в приложении минимален.

## Что умеет интерфейс

- периоды **24 часа**, **7 дней**, **2 недели**, **месяц** и произвольный диапазон с точностью до времени;
- часовая шкала и непрерывное отображение рыночной линии; перерывы торгов соединяются отдельным пунктиром;
- точные значения по наведению, касанию, клавишам стрелок и в закреплённом инспекторе под графиком;
- топ-3 лучших часов в выбранном периоде для двух операций:
  - купить USD у Freedom: `Freedom sell − USDRUBF`;
  - продать USD Freedom: `USDRUBF − Freedom buy`;
- маркеры лучших часов на графике и часовая таблица синхронных наблюдений;
- хранение до 190 дней данных.

## Частота обновления

Freedom Bank не публикует фиксированный регламент изменения валютного курса. Поэтому workflow выполняет независимый замер **каждый час в 17-ю минуту**. Минута выбрана не на границе часа, поскольку scheduled workflow может стартовать с задержкой во время пиковой нагрузки GitHub Actions.

Официальный JSON API Freedom возвращает текущую пару, но не публикует время последнего изменения. Поэтому сборщик сохраняет собственное точное время наблюдения `observedAt`. Даже если значение не изменилось, очередное часовое наблюдение всё равно остаётся в истории. После накопления данных фактическую частоту изменений курса можно оценить по моментам смены `buy` и `sell`.

## Откуда берутся данные

Текущий курс берётся напрямую из официального JSON API страницы Freedom Bank:

```text
GET https://bankffin.kz/api/exchange-rates/getRates

data.mobile[]:
  buyCode  = USD
  sellCode = RUB
```

Это точная мобильная пара `USD/RUB`; для новых наблюдений используется:

```text
method   = direct
provider = bankffin.kz
source   = official-api
```

В стартовой исторической выборке также присутствуют точки `derived-cross`, восстановленные через опубликованные `USD/KZT` и `RUB/KZT`. Они оставлены только как расчётная предыстория и визуально отличаются от точных наблюдений:

```text
Freedom покупает USD/RUB = USD/KZT покупка ÷ RUB/KZT продажа
Freedom продаёт USD/RUB = USD/KZT продажа ÷ RUB/KZT покупка
```

Часовые OHLC-свечи `USDRUBF` загружаются через MOEX ISS с `interval=60`. В момент каждого сбора сохраняется отдельная синхронная точка: текущий курс Freedom плюс последнее доступное значение часовой свечи. Сравнение не создаётся, если рынок закрыт и свежей свечи нет.

## Формат данных

`data/rates.json`, schema version 2:

- `freedom` — все ежечасные наблюдения банковского курса, включая часы без изменения;
- `market` — часовые свечи USDRUBF;
- `comparisons` — синхронные ежечасные наблюдения, по которым рассчитываются лучшие часы;
- `meta` — часовой пояс, период хранения и частота сбора.

Старые дневные точки schema version 1 автоматически мигрируют при первом запуске сборщика.

## Важно о сравнении

`USDRUBF` — биржевой вечный фьючерс, а не банковский курс обмена. Он используется как рыночный ориентир; банк не обязан проводить обмен по цене фьючерса. Для принятия решения следует смотреть отдельно направление покупки и направление продажи USD.

## Источники

- Freedom Bank: <https://bankffin.kz/ru/exchange-rates>
- официальный API Freedom Bank: <https://bankffin.kz/api/exchange-rates/getRates>
- РБК Quote, USDRUBF: <https://www.rbc.ru/quote/ticker/338247>
- MOEX ISS: <https://iss.moex.com/iss/reference/155>
- стартовый архив USD/KZT: <https://themoney.kz/banks/freedom-finance-bank/usd/>
- стартовый архив RUB/KZT: <https://themoney.kz/banks/freedom-finance-bank/rub/>

## Локальный запуск

Требуется Node.js 22+.

```bash
npm run check
npm run build
python3 -m http.server 8000 -d _site
```

Затем открыть `http://localhost:8000`.

Обновление данных вручную:

```bash
npm run update
```

## Публикация

Workflow `Update exchange rates` обновляет `data/rates.json` каждый час. После успешного обновления workflow `Deploy GitHub Pages` собирает и публикует актуальную версию сайта. В настройках репозитория нужно один раз выбрать **Settings → Pages → Source → GitHub Actions**.
